"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getNodes = void 0;

const getNodes = (node, type) => {
  const output = [];

  if (node.type === type) {
    output.push(node);
  }

  if (node.children) {
    node.children.forEach(child => output.push(...getNodes(child, type)));
  }

  return output;
};

exports.getNodes = getNodes;